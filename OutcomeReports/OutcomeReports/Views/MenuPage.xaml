<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="OutcomeReports.Views.MenuPage"
             xmlns:l="clr-namespace:OutcomeReports.Converters"
             Title="Menu">

    <ContentPage.Resources>
        <l:ConvertDateTimeToDateOnly x:Key="converter"/>
    </ContentPage.Resources>
    <!--<ContentPage.BindingContext>
        <viewModels:InitialScreenViewModel />
    </ContentPage.BindingContext>-->
    
    <StackLayout VerticalOptions="FillAndExpand">
        <Button Text="Periods" Command="{Binding PeriodPage}" />
        <Button Text="Categories" Command="{Binding CategoryPage}" />
        <Button Text="Reports" Command="{Binding ReportPage}" />
    </StackLayout>

</ContentPage>